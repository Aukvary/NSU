#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    char* s;
    scanf("%s", &s);
    
    for (int i = 0; i * 8 < n; i++) {
        for (int j = (i - 1) * 8; j < i * 8; j++) {
            
        }
    }
}