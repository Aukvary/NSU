#include <stdio.h>

#define MAX(a, b) ((a) > (b) ? a : b)

int main() {
    int n;

    scanf("%d", &n);

    int sizes[n];

    for (int i = 0; i < n; i++)
        scanf("%d", &sizes[i]);

    int l = 0;
    int r = n - 1;
    int max = 0;

    while (l < r) {
        
    }
}