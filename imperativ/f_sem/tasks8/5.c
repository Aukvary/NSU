#include <stdio.h>

int bin_search(int* nums, int len, int target) {
    int l = 0;
    int r = len - 1;

    int idx;
    int val;

    while (l <= r) {
        m = l + (r - l) / 2;
    }
}

int main() {


    return 0;
}